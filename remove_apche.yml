---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    apt:
      update_cache: yes

  - name: uninstall apache
    apt:
      name: apache2
      state: absent

  - name: remove php support of apache
    apt:
      name: libapache2-mod-php
      state: absent
